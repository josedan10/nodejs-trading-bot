// const { bitfinex } = require('../config')
// const BitfinexConnection = require('../bitfinex/bfx')
// const WS = require('ws')

// const bfx = new BitfinexConnection(
//     bitfinex.bitfinexAPIKey,
//     bitfinex.bitfinexSecret
// )

describe('Tests of bitfinex API connection', () => {
    test('Connect with websocket server', async () => {
        expect(true).toBe(true)
    })
})
